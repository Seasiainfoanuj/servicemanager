<%= render(:partial => 'layouts/left') %>
<div id="main">
  <div class="container-fluid">
    <%= render(:partial => 'layouts/notice') %>
    <div class="page-header">
      <div class="pull-left">
        <h1>Contact Role Types</h1>
      </div>
      <div class="pull-right">
        <br>
        <% if can? :view, ContactRoleType %>
          <%= link_to '<i class="icon-plus-sign"></i> Add New Contact Role Type'.html_safe, new_contact_role_type_path, {:class => 'btn btn-grey'} %>
        <% end %>
      </div>
    </div>
    <div class="breadcrumbs">
      <%= render_crumbs %>
      <div class="close-bread">
        <a href="#"><i class="icon-remove"></i></a>
      </div>
    </div>
    <div class="row-fluid">
      <div class="span12">
        <div class="box box-condensed box-color box-bordered">
          <div class="box-title">
            <h3><i class="glyphicon-user"></i>Contact Role Types</h3> %>
          </div>
          <div class="box-content nopadding">
            <table class="table table-hover table-nomargin dataTable dataTable-tools dataTable-colvis dataTable-scroll-x table-bordered">
              <thead>
                <tr>
                  <th>Contact Role Name</th>
                  <th width="100">Actions</th>
                </tr>
              </thead>
              <tbody>
                <% @contact_role_types.each do |contact_role_type| %>
                  <tr>
                    <td><%= contact_role_type.name %></td>
                    <td>
                      <% if can? :update, ContactRoleType %>
                        <%= link_to '<i class="icon-edit"></i>'.html_safe, edit_contact_role_type_path(contact_role_type), {:title => 'Edit', :class => 'btn', 'rel' => 'tooltip'} %>
                      <% end %>
                      <% if can? :destroy, ContactRoleType %>
                        <%= link_to '<i class="icon-ban-circle"></i>'.html_safe, contact_role_type, method: :delete, :class => 'btn hidden', :title => 'Destroy', 'rel' => 'tooltip', data: {confirm: "You are about to permanently delete #{contact_role_type.name}. You cannot reverse this action. Are you sure you want to proceed?"} %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

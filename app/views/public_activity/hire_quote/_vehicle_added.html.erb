<% activity.trackable ? uid = activity.parameters[:uid] || activity.trackable.reference || activity.trackable.id : uid = activity.parameters[:uid] || "[now deleted]" %>
<%= render 'public_activity/shared/activity.html.erb', activity: activity, display: display, action: "Hire Product #{activity.parameters[:vehicle_model_name]} added to quote", uid: uid %>

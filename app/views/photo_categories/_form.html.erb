<%= form_for(@photo_category, :html => {:id => "photo-category-form", :class => "form-horizontal"}) do |f| %>
  <% if @photo_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_category.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
        <% @photo_category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="box-title">
    <h3><i class="glyphicon-notes"></i>Photo Category Details</h3>
  </div>
  <div class="box-content">
    <div class="control-group">
      <%= f.label :name, "Name", :class => "control-label" %>
      <div class="controls">
        <%= f.text_field :name, :class => "input-xlarge" %>
      </div>
    </div>
  </div>
<% end %>

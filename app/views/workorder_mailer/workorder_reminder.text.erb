Hi <%= @user.first_name %>,

This is a reminder that the following workorder is <%= @workorder.status %> and scheduled for <%= @workorder.sched_date_field %> - <%= @workorder.sched_time_field %>. You can login and view this workorder here: <%= workorder_url(@workorder) %>

<%= render 'workorder' %>
